<Window x:Class="SimpleDroneGCS.UI.Dialogs.SRTMDownloadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Загрузка рельефа SRTM"
        Width="420" Height="380"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Border Background="#0D1733" CornerRadius="12" BorderBrush="#2A4361" BorderThickness="2">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                <TextBlock Text="⛰️" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Загрузка данных рельефа" 
                           Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Выбор региона -->
            <TextBlock Grid.Row="1" Text="Регион:" Foreground="#8899AA" FontSize="12" Margin="0,0,0,5"/>
            <ComboBox Grid.Row="2" x:Name="RegionComboBox" 
                      Background="#1A2433" Foreground="#1A2433" 
                      FontSize="13" Height="35" Margin="0,0,0,15"
                      SelectionChanged="RegionComboBox_SelectionChanged">
                <ComboBoxItem Content="🇰🇿 Весь Казахстан (~670 тайлов)" Tag="KZ"/>
                <ComboBoxItem Content="🏙️ Алматы и область" Tag="Almaty"/>
                <ComboBoxItem Content="🏙️ Астана и область" Tag="Astana"/>
                <ComboBoxItem Content="🏙️ Шымкент и область" Tag="Shymkent"/>
                <ComboBoxItem Content="📍 Текущая позиция карты" Tag="Current" IsSelected="True"/>
                <ComboBoxItem Content="✏️ Указать вручную" Tag="Custom"/>
            </ComboBox>

            <!-- Инфо о размере -->
            <Border Grid.Row="3" Background="#1A2433" CornerRadius="6" Padding="10" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock x:Name="TilesInfoText" Text="Тайлов: ~4" Foreground="White" FontSize="12"/>
                    <TextBlock x:Name="SizeInfoText" Text="Примерный размер: ~11 MB" Foreground="#8899AA" FontSize="11" Margin="0,3,0,0"/>
                    <TextBlock x:Name="CacheInfoText" Text="В кэше: 0 MB" Foreground="#98F019" FontSize="11" Margin="0,3,0,0"/>
                </StackPanel>
            </Border>

            <!-- Прогресс -->
            <StackPanel Grid.Row="4" x:Name="ProgressPanel" Visibility="Collapsed">
                <TextBlock x:Name="ProgressText" Text="Загрузка..." Foreground="White" FontSize="12" Margin="0,0,0,8"/>
                <ProgressBar x:Name="ProgressBar" Height="8" Minimum="0" Maximum="100" 
                             Background="#1A2433" Foreground="#98F019" BorderThickness="0"/>
                <TextBlock x:Name="ProgressDetailText" Text="" Foreground="#8899AA" FontSize="11" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Кнопки -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button x:Name="CancelButton" Content="Отмена" Width="100" Height="35"
                        Background="#2A4361" Foreground="White" BorderThickness="0"
                        FontSize="12" Cursor="Hand" Click="CancelButton_Click"/>
                <Button x:Name="DownloadButton" Content="Скачать" Width="120" Height="35"
                        Background="#98F019" Foreground="#0D1733" BorderThickness="0"
                        FontSize="12" FontWeight="Bold" Margin="10,0,0,0" Cursor="Hand" 
                        Click="DownloadButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>