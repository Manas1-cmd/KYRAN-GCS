<Window x:Class="SimpleDroneGCS.Views.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="500" Width="500"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True">

    <Grid>
        <!-- ВНЕШНИЙ КРУГ (градиент космический) -->
        <Ellipse Width="420" Height="420">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="#060B1A" Offset="0"/>
                    <GradientStop Color="#0D1733" Offset="0.5"/>
                    <GradientStop Color="#060B1A" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
            <Ellipse.Effect>
                <!-- БЕЛОЕ СВЕЧЕНИЕ -->
                <DropShadowEffect Color="White" BlurRadius="40" ShadowDepth="0" Opacity="0.5"/>
            </Ellipse.Effect>
        </Ellipse>

        <!-- КОНТЕНТ -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <!-- ЛОГО (опущено на 20px) -->
            <Grid Width="160" Height="160" Margin="0,20,0,30">
                <Image Source="/Images/drone_icon.png" 
                       Width="140" 
                       Height="135"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center">
                    <Image.Effect>
                        <DropShadowEffect Color="#98F019" BlurRadius="30" ShadowDepth="0" Opacity="0.8"/>
                    </Image.Effect>
                </Image>
            </Grid>

            <!-- НАЗВАНИЕ SQK GCS -->
            <TextBlock Text="S Q K   G C S"
                       FontSize="34"
                       FontWeight="Black"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,8">
                <TextBlock.Effect>
                    <DropShadowEffect Color="White" BlurRadius="20" ShadowDepth="0" Opacity="0.6"/>
                </TextBlock.Effect>
            </TextBlock>

            <!-- PRESENTED BY SPACEY - ЗЕЛЁНЫЙ -->
            <TextBlock Text="presented by spacey"
                       FontSize="14"
                       FontStyle="Italic"
                       FontWeight="SemiBold"
                       Foreground="#98F019"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,35">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#98F019" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                </TextBlock.Effect>
            </TextBlock>

            <!-- LOADING DOTS ANIMATION -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center">
                <Ellipse Width="10" Height="10" Fill="#98F019" Margin="6" x:Name="Dot1" Opacity="0.3"/>
                <Ellipse Width="10" Height="10" Fill="#98F019" Margin="6" x:Name="Dot2" Opacity="0.3"/>
                <Ellipse Width="10" Height="10" Fill="#98F019" Margin="6" x:Name="Dot3" Opacity="0.3"/>
            </StackPanel>

        </StackPanel>
    </Grid>

    <!-- АНИМАЦИЯ -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <!-- Fade in окна -->
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                   From="0" To="1" Duration="0:0:0.6"/>

                    <!-- Пульсация точек загрузки -->
                    <DoubleAnimation Storyboard.TargetName="Dot1"
                                   Storyboard.TargetProperty="Opacity"
                                   From="0.3" To="1" Duration="0:0:0.5"
                                   AutoReverse="True" RepeatBehavior="Forever"/>
                    <DoubleAnimation Storyboard.TargetName="Dot2"
                                   Storyboard.TargetProperty="Opacity"
                                   From="0.3" To="1" Duration="0:0:0.5"
                                   AutoReverse="True" RepeatBehavior="Forever"
                                   BeginTime="0:0:0.2"/>
                    <DoubleAnimation Storyboard.TargetName="Dot3"
                                   Storyboard.TargetProperty="Opacity"
                                   From="0.3" To="1" Duration="0:0:0.5"
                                   AutoReverse="True" RepeatBehavior="Forever"
                                   BeginTime="0:0:0.4"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>