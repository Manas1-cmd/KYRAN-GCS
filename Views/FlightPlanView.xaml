<UserControl x:Class="SimpleDroneGCS.Views.FlightPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:gmap="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
             Background="{StaticResource SpaceBlack}"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="420"/>
        </Grid.ColumnDefinitions>

        <!-- ЛЕВАЯ ЧАСТЬ: КАРТА ДЛЯ ПЛАНИРОВАНИЯ -->
        <Border Grid.Column="0"
                Background="{StaticResource DarkPanel}"
                BorderBrush="{StaticResource BlueGray}"
                BorderThickness="2"
                CornerRadius="12"
                ClipToBounds="True"
                Margin="0,0,10,0">
            <Grid>
                <!-- Карта -->
                <gmap:GMapControl x:Name="PlanMap"
                                  Background="Transparent"
                                  Panel.ZIndex="0"
                                  Zoom="17"
                                  MinZoom="2"
                                  MaxZoom="20"
                                  MouseWheelZoomEnabled="True"
                                  MouseWheelZoomType="MousePositionAndCenter"
                                  MouseDoubleClick="PlanMap_MouseDoubleClick"
                                  MouseWheel="PlanMap_MouseWheel"/>
                <!-- OVERLAY: Дистанция (правый нижний угол карты) -->
                <Border HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Margin="0,0,12,12"
                        Background="#CC0D1733"
                        BorderBrush="#2A4361"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="8,5"
                        Panel.ZIndex="100">
                    <StackPanel>
                        <TextBlock x:Name="TotalDistanceOverlay"
                                   Text="Маршрут: 0 м"
                                   Foreground="White"
                                   FontSize="10"
                                   FontWeight="Bold"
                                   Margin="0,0,0,3"/>
                        <Border Background="#1A2433" Height="1" Margin="0,0,0,3"/>
                        <TextBlock x:Name="CursorDistanceFromLast"
                                   Text="От WP: —"
                                   Foreground="#4ADE80"
                                   FontSize="10"
                                   FontWeight="SemiBold"/>
                        <TextBlock x:Name="CursorDistanceFromHome"
                                   Text="От HOME: —"
                                   Foreground="#F59E0B"
                                   FontSize="10"
                                   FontWeight="SemiBold"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
                <!-- OVERLAY: Координаты курсора (правый верхний угол) -->
                <Border HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,15,12,0"
                        Background="#CC0D1733"
                        BorderBrush="#2A4361"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="8,5"
                        Panel.ZIndex="100">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="LAT: "
                                       Foreground="{StaticResource TextGray}"
                                       FontSize="10"/>
                            <TextBlock x:Name="CursorLatText"
                                       Text="—"
                                       Foreground="White"
                                       FontSize="10"
                                       FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                            <TextBlock Text="LNG: "
                                       Foreground="{StaticResource TextGray}"
                                       FontSize="10"/>
                            <TextBlock x:Name="CursorLngText"
                                       Text="—"
                                       Foreground="White"
                                       FontSize="10"
                                       FontWeight="SemiBold"/>
                        </StackPanel>
                        <Border Background="#1A2433" Height="1" Margin="0,3,0,3"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="ALT: "
                                       Foreground="{StaticResource TextGray}"
                                       FontSize="10"/>
                            <TextBlock x:Name="CursorAltText"
                                       Text="— м"
                                       Foreground="#4ADE80"
                                       FontSize="10"
                                       FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- СЕЛЕКТОР ТИПА АППАРАТА (СВЕРХУ СЛЕВА, КАК В FlightDataView) -->
                <Border x:Name="VehicleTypeSelector"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="15"
                        Padding="8,6"
                        CornerRadius="8"
                        Background="#0D1733"
                        BorderBrush="{StaticResource BlueGray}"
                        BorderThickness="2"
                        Panel.ZIndex="200"
                        Cursor="Hand"
                        MouseLeftButtonDown="VehicleTypeSelector_MouseLeftButtonDown">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Короткая метка -->
                        <Border Grid.Column="0"
                                Background="#122244"
                                CornerRadius="5"
                                Padding="5,2"
                                Margin="0,0,6,0">
                            <TextBlock x:Name="VehicleTypeShortText"
                                       Text="MC"
                                       Foreground="{StaticResource AcidGreen}"
                                       FontWeight="Bold"
                                       FontSize="10"/>
                        </Border>

                        <!-- Полное название -->
                        <TextBlock Grid.Column="1"
                                   x:Name="VehicleTypeFullText"
                                   Text="Мультикоптер"
                                   Foreground="White"
                                   FontSize="11"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0"/>

                        <!-- Стрелка -->
                        <TextBlock Grid.Column="2"
                                   Text="▼"
                                   Foreground="{StaticResource AcidGreen}"
                                   FontSize="10"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- ПОЛЗУНОК ЗУМА (справа) -->
                <Border HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="#0D1733"
                        BorderBrush="{StaticResource BlueGray}"
                        BorderThickness="2"
                        CornerRadius="8"
                        Padding="0,8"
                        Margin="15"
                        Width="42"
                        Panel.ZIndex="100">
                    <StackPanel Width="36">
                        <TextBlock Text="ЗУМ"
                                   Foreground="White"
                                   FontSize="9"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,4"/>

                        <Slider x:Name="ZoomSlider"
                                Orientation="Vertical"
                                Minimum="2"
                                Maximum="20"
                                Value="15"
                                Height="140"
                                TickFrequency="1"
                                IsSnapToTickEnabled="True"
                                ValueChanged="ZoomSlider_ValueChanged"
                                HorizontalAlignment="Center"/>

                        <TextBlock Text="{Binding ElementName=ZoomSlider, Path=Value, StringFormat='{}{0:F0}'}"
                                   Foreground="White"
                                   FontSize="10"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- ВЫБОР КАРТЫ + НАСТРОЙКИ (внизу слева) -->
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Bottom"
                            HorizontalAlignment="Left"
                            Margin="15"
                            Panel.ZIndex="10">

                    <ComboBox x:Name="MapTypeCombo"
                              Style="{StaticResource CustomComboBoxStyle}"
                              ItemContainerStyle="{StaticResource CustomComboBoxItemStyle}"
                              SelectionChanged="MapTypeCombo_SelectionChanged"
                              Height="36">
                        <ComboBoxItem Content="Google Спутник" Tag="GoogleSatellite" IsSelected="True"/>
                        <ComboBoxItem Content="Google Карта" Tag="GoogleMap"/>
                        <ComboBoxItem Content="OpenStreetMap" Tag="OpenStreetMap"/>
                        <ComboBoxItem Content="Bing Спутник" Tag="BingSatellite"/>
                        <ComboBoxItem Content="Bing Карта" Tag="BingMap"/>
                    </ComboBox>

                    <!-- Скачать карту офлайн -->
                    <Button Content="📥" 
                            ToolTip="Скачать карту офлайн"
                            Width="36" Height="36"
                            Margin="8,0,0,0"
                            Background="#0D1733"
                            Foreground="White"
                            FontSize="16"
                            BorderThickness="2"
                            BorderBrush="#2A4361"
                            Cursor="Hand"
                            Click="DownloadMapButton_Click"/>

                    <!-- Радиус меток -->
                    <Border HorizontalAlignment="Center"
                            Background="{DynamicResource MidBlue}"
                            BorderThickness="2"
                            CornerRadius="6"
                            Padding="8"
                            Margin="10,0,0,0"
                            Panel.ZIndex="10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Радиус:"
                                       Foreground="{StaticResource White}"
                                       FontSize="10"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <Slider x:Name="RadiusSlider"
                                    Minimum="10"
                                    Maximum="100"
                                    Value="30"
                                    Width="80"
                                    VerticalAlignment="Center"
                                    ValueChanged="RadiusSlider_ValueChanged"/>
                            <TextBlock Text="{Binding ElementName=RadiusSlider, Path=Value, StringFormat='{}{0:F0} м'}"
                                       Foreground="{StaticResource AcidGreen}"
                                       FontSize="10"
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

            </Grid>
        </Border>

        <!-- ПРАВАЯ ЧАСТЬ: СПИСОК WAYPOINTS -->
        <Border Grid.Column="1"
                Background="{StaticResource DarkBlue}"
                BorderBrush="{StaticResource MidBlue}"
                BorderThickness="2"
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ЗАГОЛОВОК -->
                <Border Grid.Row="0"
                        CornerRadius="5,5,0,0"
                        Background="{StaticResource MidBlue}"
                        Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="МАРШРУТНЫЕ ТОЧКИ"
                                   Foreground="{StaticResource AcidGreen}"
                                   FontSize="13"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"/>

                        <TextBlock Grid.Column="1"
                                   x:Name="WaypointsCountText"
                                   Text="Точек: 0"
                                   Foreground="{StaticResource TextGray}"
                                   FontSize="10"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0"/>
                    </Grid>
                </Border>

                <!-- ТАБЛИЦА WAYPOINTS -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="4">

                        <!-- Заголовки колонок -->
                        <Border Background="#1A2433" Padding="6,4" Margin="0,0,0,2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="#" Foreground="{StaticResource TextGray}" FontSize="9" FontWeight="Bold"/>
                                <TextBlock Grid.Column="1" Text="Команда" Foreground="{StaticResource TextGray}" FontSize="9" FontWeight="Bold" Margin="4,0,40,0"/>
                                <TextBlock Grid.Column="1" Text="Высота" Foreground="{StaticResource TextGray}" FontSize="9" FontWeight="Bold" Margin="130,0,30,0" Grid.ColumnSpan="2"/>
                                <TextBlock Grid.Column="2" Text="Задерж" Foreground="{StaticResource TextGray}" FontSize="9" FontWeight="Bold" Margin="16,0,10,0" Grid.ColumnSpan="2"/>
                                <TextBlock Grid.Column="4" Text="Действия" Foreground="{StaticResource TextGray}" FontSize="9" FontWeight="Bold" HorizontalAlignment="Left" Width="80" Margin="20,0,0,0"/>
                            </Grid>
                        </Border>

                        <!-- TAKEOFF блок (фиксированный) -->
                        <Border Background="#0D1733" 
                                BorderBrush="#2A4361" 
                                BorderThickness="1" 
                                CornerRadius="5" 
                                Padding="8,6" 
                                Margin="0,0,0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Path x:Name="TakeoffIcon" Data="M12,2 L4.5,20.29 L5.21,20.59 L12,18 L18.79,20.59 L19.5,20.29 L12,2Z"
                                          Fill="#4ADE80" Width="14" Height="14" Stretch="Uniform" Margin="0,0,6,0"/>
                                    <TextBlock x:Name="TakeoffLabel" Text="ВЗЛЁТ" Foreground="#4ADE80" FontWeight="Bold" FontSize="11"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Horizontal">
                                    <TextBlock Text="Высота:" Foreground="{StaticResource TextGray}" 
                                                FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBox x:Name="TakeoffAltitudeBox"
                                                 Text="10"
                                                 Width="45" Height="24"
                                                 FontSize="10"
                                                 Background="#1A2433"
                                                 Foreground="White"
                                                 BorderBrush="#2A4361"
                                                 BorderThickness="1"
                                                 Padding="4,0"
                                                 VerticalContentAlignment="Center"/>
                                    <TextBlock Text="м" Foreground="{StaticResource TextGray}" 
                                                FontSize="10" VerticalAlignment="Center" Margin="2,0,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Динамический список waypoints -->
                        <StackPanel x:Name="WaypointsListPanel"/>

                        <!-- RTL блок (фиксированный) -->
                        <Border Background="#0D1733" 
                                BorderBrush="#2A4361" 
                                BorderThickness="1" 
                                CornerRadius="5" 
                                Padding="8,6" 
                                Margin="0,4,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Path x:Name="RtlIcon" Data="M12,3 L2,12 L5,12 L5,20 L11,20 L11,14 L13,14 L13,20 L19,20 L19,12 L22,12 Z"
                                            Fill="#F59E0B" Width="14" Height="14" Stretch="Uniform" Margin="0,0,6,0"/>
                                    <TextBlock x:Name="RtlLabel" Text="ВОЗВРАТ (RTL)" Foreground="#F59E0B" FontWeight="Bold" FontSize="11"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Horizontal">
                                    <TextBlock Text="Высота:" Foreground="{StaticResource TextGray}" 
                                                FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBox x:Name="RtlAltitudeBox"
                                                 Text="15"
                                                 Width="45" Height="24"
                                                 FontSize="10"
                                                 Background="#1A2433"
                                                 Foreground="White"
                                                 BorderBrush="#2A4361"
                                                 BorderThickness="1"
                                                 Padding="4,0"
                                                 VerticalContentAlignment="Center"
                                                 />
                                    <TextBlock Text="м" Foreground="{StaticResource TextGray}" 
                                                FontSize="10" VerticalAlignment="Center" Margin="2,0,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <!-- КНОПКИ УПРАВЛЕНИЯ -->
                <StackPanel Grid.Row="2" Margin="8" Background="{StaticResource DarkBlue}">


                    <!-- Управление миссией -->
                    <Border BorderBrush="{StaticResource BlueGray}"
                            BorderThickness="0,1,0,0"
                            Padding="0,8,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- HOME -->
                            <Button Grid.Row="0"
                                    Grid.ColumnSpan="2"
                                    Style="{StaticResource DroneButtonStyle}"
                                    Background="#EF4464"
                                    Height="32"
                                    FontSize="11"
                                    Margin="0,0,0,6"
                                    Click="SetHomeButton_Click">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Image Source="/Images/home_icon.png" Width="14" Height="14" Margin="0,0,6,0"/>
                                    <TextBlock Text="Установить HOME" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <!-- Записать / Читать -->
                            <Button Grid.Row="1" Grid.Column="0"
                                    Content="Записать"
                                    Style="{StaticResource GreenButtonStyle}"
                                    Height="32"
                                    FontSize="11"
                                    Margin="0,0,2,4"
                                    Click="UploadButton_Click"/>

                            <Button Grid.Row="1" Grid.Column="1"
                                    Content="Читать"
                                    Style="{StaticResource DroneButtonStyle}"
                                    Height="32"
                                    FontSize="11"
                                    Margin="2,0,0,4"
                                    Click="DownloadButton_Click"/>

                            <!-- Сохранить / Загрузить -->
                            <Button Grid.Row="2" Grid.Column="0"
                                    Content="Сохранить"
                                    Style="{StaticResource DroneButtonStyle}"
                                    Height="30"
                                    FontSize="10"
                                    Margin="0,0,2,0"
                                    Click="SaveButton_Click"/>

                            <Button Grid.Row="2" Grid.Column="1"
                                    Content="Загрузить"
                                    Style="{StaticResource DroneButtonStyle}"
                                    Height="30"
                                    FontSize="10"
                                    Margin="2,0,0,0"
                                    Click="LoadButton_Click"/>
                        </Grid>
                    </Border>

                    <!-- Очистить + SRTM в одну строку -->
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                Content="Очистить"
                                Style="{StaticResource DroneButtonStyle}"
                                Background="#EF4444"
                                Height="30"
                                FontSize="10"
                                Margin="0,0,2,0"
                                Click="ClearButton_Click"/>

                        <Button Grid.Column="1"
                                Content="⛰️ Рельеф"
                                Style="{StaticResource DroneButtonStyle}"
                                Background="#2A4361"
                                Height="30"
                                FontSize="10"
                                Margin="2,0,0,0"
                                Click="DownloadSRTM_Click"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
